
# This script does the initial setup of the project tree so that you can then do generates and builds.
# You only need to run this script once at the beginning or whenever you change the root project_setup.xml file.


java -cp lib/java/coreutil.jar:lib/java/code_generator.jar codegenerator.generator.CodeGenerator_Main CodeGeneratorConfig.xml ../ArchTemplates/project/project_setup.top.template project_setup.xml
